# mali-chat

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

This project was created using `bun init` in bun v1.3.14. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.

only install dependencies on the root of project to not creating any extra node_modules.

to open a simulator:
`xcrun simctl boot "iPhone SE (3rd generation)"`

and close other one and run `--ios -c` to see on new device too.

bun create expo-app mobile -> 54 -> yes for git

bunx tailwindcss init instead of npx tailwindcss init
